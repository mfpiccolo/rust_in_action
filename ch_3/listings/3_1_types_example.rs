fn main() {
    let five = 5;
    lotta_types(five, "thing".to_string(), vec!("ldsfkj"));
}

fn lotta_types(i: i32, s: String, collection: Vec<&str>) -> Vec<&str> {
  let squared = i.pow(2);
  collection
}
